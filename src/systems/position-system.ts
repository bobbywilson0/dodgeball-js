class PositionSystem {
    constructor(entities) {
        for (let id in entities) {
            const currentEntity = entities[id];

            if (currentEntity.components.PositionComponent) {
                
            }
        }
    }
}